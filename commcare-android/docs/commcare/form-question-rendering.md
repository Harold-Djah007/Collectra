# Form question rendering

Collectra Mobile renders the XForm labels, hints, options, and constraints configured in HQ. Markdown-only question labels remain visible on mobile even when the plain text label is absent. This changes presentation only; the form XML, answers, and case updates are unaffected.
